import 'package:flutter/material.dart';

class DisplayInvoiceBodyView extends StatelessWidget {
  const DisplayInvoiceBodyView({super.key});

  @override
  Widget build(BuildContext context) {
    return const CustomScrollView(
      slivers: [],
    );
  }
}
